#include "ladder.h"


int main() {
    
    // For now lets skip the taking two input words, then calling load words, then generating and printing ladder
    // Just make sure it works
    verify_word_ladder();
    
    return 0;
}